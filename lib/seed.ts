import { RawCompanyMention } from "@/types/company";

// Production must never fall back to seeded/example leads.
// Real data is produced by scripts/* and read from LEADGRID_DATA_DIR.
export const rawCompanyMentions: RawCompanyMention[] = [];
